@using Restaurant.PL.ViewModels.Auth_ViewModels
@model ForgetPasswordViewModel
@{
    ViewData["Title"] = "Forget Password";
    Layout = "~/Views/Shared/_AuthLayout.cshtml";
}

<div class="row justify-content-center align-items-center" style="min-height: 70vh;">
    <div class="col-md-6 col-lg-5">
        <div class="card shadow-sm" style="border-radius: 16px;">
            <div class="card-body p-5">
                <div class="text-center mb-4">
                    <i class="bi bi-envelope-lock" style="font-size: 3rem; color: #ff7f27;"></i>
                    <h2 class="fw-bold mt-2" style="color: #ff7f27;">Forgot your password?</h2>
                    <p class="text-muted mb-0">Enter your email address and we'll send you a link to reset your password.</p>
                </div>
                <form asp-action="SendResetPassword" method="post" autocomplete="off">
                    <div class="mb-3">
                        <label asp-for="Email" class="form-label fw-semibold" style="color: #ff7f27;"></label>
                        <input asp-for="Email" class="form-control form-control-lg" placeholder="Enter your email" />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>
                    <button type="submit" class="btn w-100 py-2 fw-bold" style="background-color: #ff7f27; color: #fff; border-radius: 8px;">
                        <i class="bi bi-send me-1"></i> Send Reset Link
                    </button>
                </form>
                <div class="mt-4 text-center">
                    <a asp-action="Login" asp-controller="Account" style="color: #ff7f27; text-decoration: underline;">
                        <i class="bi bi-arrow-left"></i> Back to Login
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}



